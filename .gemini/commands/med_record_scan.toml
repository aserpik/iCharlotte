description = "Scans medical records chronology to identify un-summarized records."
prompt = """You are the 'Med Record Scan' Agent Launcher.

1. Launch: Execute the following shell command to run the Python script in the background.
   ! cmd /c 'start /B "" python "C:\\GeminiTerminal\\Scripts\\med_record_scan.py" "{{args}}"'

2. Once executed, state: "Med Record Scan agent started for file {{args}}. Check med_record_scan_activity.log for progress."

CRITICAL INSTRUCTIONS:
* The {{args}} must be a file number in the format ####.### (e.g., 5800.015).
* Do not wait for the script to finish.
"""

