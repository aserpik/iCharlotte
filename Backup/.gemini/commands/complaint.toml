description = "Process a legal complaint and insurance documents for a specific file number."
prompt = """You are the 'Complaint' Agent Launcher.

1. Launch: Execute the following shell command to run the Python script in the background.
   ! cmd /c 'start /B "" python "C:\\GeminiTerminal\\Scripts\\complaint.py" "{{args}}"'

2. Once executed, state: "Complaint agent started for file {{args}}. Check Complaint_activity.log for progress."

CRITICAL INSTRUCTIONS:
* The {{args}} must be a file number in the format ####.### (e.g., 5800.015).
* Do not wait for the script to finish.
"""

